package com.jm.online_store.model.filter;

/**
 * Enum для видов фильтров
 * формат строчных букв - требование фронтов (прописные вызовут ошибку)
 */
public enum FilterType {
    Range,
    Checkbox
}

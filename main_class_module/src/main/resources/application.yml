spring:
  session:
    jdbc:
      initialize-schema: always
      table-name: SPRING_SESSION
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/online_store?useUnicode=yes&characterEncoding=UTF-8&useSSL=false&serverTimezone=Europe/Moscow
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL57Dialect
        storage_engine: innodb
        jdbc:
          timezone: UTC
  output:
    ansi:
      enabled: ALWAYS
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  mail:
    host: smtp.yandex.ru
    username: jm-online-store@yandex.ru
    password: jm2020
    port: 465
    protocol: smtps
    debug: true
  server:
    url: http://localhost:9999
  mvc:
    static-path-pattern: /static/**
server:
  port: 9999
logging:
  file:
    name: src/main/logs/my.log
    max-history=1: 1
  level:
    com:
      jm: debug
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %logger{36} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'
emailStockSending:
  delay: '0 0 10 */1 * *'

price-list-service:
  path-to-xls-file: uploads/files/prices/pricedaily.xls
  path-to-zip-file: uploads/files/prices/pricedaily.zip

spring.profiles.include: 'security'

# Смотри инструкцию https://trello.com/c/8akD9MhL
telegram-bot:
  name: online_store_stocks_bot
  token: 1405685131:AAF7CiyhMfTgtkhbxIbAPWt1kipZPRxPuC8
  webhook-path:  https://671419db14cf.ngrok.io/telegram-bot